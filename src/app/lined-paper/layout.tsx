import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Lined Paper Generator — Printable Polly",
  description: "Generate custom ruled/lined paper with college, wide, or narrow spacing. Add margins, change line colors, and print instantly.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

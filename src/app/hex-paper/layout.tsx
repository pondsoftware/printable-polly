import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Hexagonal Graph Paper — Printable Polly",
  description: "Generate free printable hexagonal graph paper with adjustable hex size, line color, and line weight. Perfect for chemistry, RPG maps, and design projects.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

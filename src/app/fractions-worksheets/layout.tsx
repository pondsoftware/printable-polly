import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Fractions Worksheets — Add, Subtract & Simplify — Printable Polly",
  description: "Free printable fractions worksheets. Practice adding, subtracting, and simplifying fractions with mixed numbers. Generate random problems with answer keys — no signup required.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Sight Word Practice Sheets — Printable Polly",
  description: "Generate free printable sight word practice sheets by grade level. Kids trace and write high-frequency words with dotted guides for Pre-K through 2nd grade.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

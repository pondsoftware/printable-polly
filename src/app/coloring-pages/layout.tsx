import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Geometric Coloring Pages — Mandalas & Patterns — Printable Polly",
  description: "Generate beautiful geometric coloring pages with mandalas, mosaics, kaleidoscopes, and tessellations. Customize complexity and line thickness, then print for free.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

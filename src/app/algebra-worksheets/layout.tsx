import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Algebra Worksheets — Solve for X — Printable Polly",
  description: "Free printable algebra worksheets. Practice solving for x with one-step, two-step, and multi-step equations including distribution. Answer keys included — no signup required.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

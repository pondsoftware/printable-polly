import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Handwriting Practice Sheets — Traceable Print & Cursive — Printable Polly",
  description: "Generate printable handwriting practice sheets with traceable text in print or cursive. Choose from alphabet, famous quotes, nursery rhymes, Aesop's fables, or type your own text.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

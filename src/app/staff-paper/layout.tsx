import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Blank Sheet Music — Staff Paper — Printable Polly",
  description: "Generate free blank sheet music staff paper with adjustable staves, clef options (treble, bass, none), and custom line colors. Print or save as PDF instantly.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Storyboard Template — Printable Polly",
  description: "Print storyboard templates with drawing panels and note areas. Great for film, animation, video projects, and creative planning.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

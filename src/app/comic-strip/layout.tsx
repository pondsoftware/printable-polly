import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Comic Strip Template — Printable Polly",
  description: "Print blank comic strip templates with customizable panel layouts and border styles. Perfect for kids, classrooms, and creative storytelling.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

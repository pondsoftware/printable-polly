import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable SMART Goal Setting Worksheet — Printable Polly",
  description: "Print a SMART goal-setting worksheet. Define specific, measurable, achievable, relevant, and time-bound goals with action steps.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

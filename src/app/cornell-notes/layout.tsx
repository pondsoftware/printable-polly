import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Cornell Notes Template — Printable Polly",
  description: "Print Cornell note-taking templates with cue column, notes section, and summary area. Customize title and guide lines.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

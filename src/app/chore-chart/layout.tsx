import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Chore Chart for Kids — Printable Polly",
  description: "Generate a printable weekly chore chart for kids. Customize chore names, number of tasks, and add a reward row. Print instantly from your browser.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

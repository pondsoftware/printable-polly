import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Printable Dot Grid Paper Generator — Printable Polly",
  description: "Generate custom dot grid paper for bullet journals and sketching. Adjust dot spacing, size, and color. Print or save as PDF instantly.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
